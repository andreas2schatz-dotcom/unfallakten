# Modul 1 – Abhängigkeiten
# Alle Pakete mit Versionsangaben für reproduzierbare Builds

# ── Webframework (Modul 2+) ──────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # Datei-Uploads

# ── Datenbank ────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.30             # ORM (für Phase 2: PostgreSQL-Migration)
alembic==1.13.1                # Migrationen
# psycopg2-binary==2.9.9       # Aktivieren wenn PostgreSQL

# ── Authentifizierung (Modul 2) ───────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # Passwort-Hashing (ersetzt pbkdf2 in Modul 1)

# ── PDF-Parsing (Modul 4) ─────────────────────────────────────────────────────
pdfplumber==0.11.1
PyMuPDF==1.24.3
openai==1.30.1                 # GPT-4o (Fallback, nicht aktiv)
anthropic==0.40.0             # Claude Sonnet – KI-Haftungsbegründung
google-genai==1.10.0          # Gemini 3.1 Pro – KI-Haftungsbegründung

# ── Word-Generierung (Modul 5) ────────────────────────────────────────────────
python-docx==1.1.2
docxtpl==0.16.8                # Jinja2-Vorlagen in .docx

# ── Datenvalidierung ─────────────────────────────────────────────────────────
pydantic==2.7.1                # Request/Response-Validierung in FastAPI

# ── Entwicklung & Tests ───────────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0
httpx==0.27.0                  # Test-Client für FastAPI

# ── RA-Micro SQL Server Connector (Modul 8) ───────────────────────────────────
pymssql==2.3.1                 # Microsoft SQL Server 2014 Verbindung
